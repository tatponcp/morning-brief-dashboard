# ความปลอดภัย

## แจ้งช่องโหว่

พบช่องโหว่หรือข้อมูลที่ไม่ควรอยู่ใน repo ให้แจ้งเจ้าของ repo โดยตรง
อย่าเปิดเป็น issue สาธารณะ เพราะ repo นี้เป็น public

## ข้อควรระวังของโปรเจกต์นี้

- **repo เป็น public** — ห้าม commit รหัสผ่าน, key, URL ภายใน, IP ของเซิร์ฟเวอร์
  หรือภาพหน้าจอที่ติดข้อมูลเหล่านี้
- `SUPABASE_SERVICE_ROLE_KEY` ข้ามสิทธิ์ทั้งหมดของฐานข้อมูล ต้องอยู่ฝั่งเซิร์ฟเวอร์เท่านั้น
  และต้องไม่ขึ้นต้นด้วย `NEXT_PUBLIC_`
- `STUDIO_PASSWORD` เป็นรหัสชุดเดียวของทีม ควรเปลี่ยนเมื่อมีคนออกจากทีม
  ถ้าไม่ได้ตั้งค่าไว้ `/studio` จะปิดตัวเองบน production
- ตาราง `briefs` เปิด RLS ไว้และไม่มี policy จึงอ่านผ่าน anon key ไม่ได้
  ถังเก็บภาพ `brief-images` เปิดสาธารณะเพราะต้องแสดงบนหน้าเว็บ อย่าอัปโหลดภาพที่มีข้อมูลลับ
- ถ้าเผลอ push ค่าลับขึ้นไปแล้ว ให้ถือว่าค่านั้นรั่วทันที ต้องเปลี่ยนค่าใหม่
  การลบ commit อย่างเดียวไม่พอ
