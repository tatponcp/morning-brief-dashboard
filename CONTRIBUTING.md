# แนวทางการพัฒนา

## ก่อนเริ่ม

```bash
cd web
npm install
npm run dev
```

อ่าน [สถาปัตยกรรม](docs/architecture.md) ก่อนแก้โค้ดชั้นข้อมูล
และ [โมเดลสัญญาณ](docs/signal-model.md) ก่อนแก้กติกาให้คะแนน

## ขั้นตอนการส่งงาน

1. แตกสาขาจาก `main` ตั้งชื่อสั้นเป็นภาษาอังกฤษ เช่น `usd-3way`, `studio-ux`
2. แก้ทีละเรื่อง หนึ่ง pull request ต่อหนึ่งเรื่อง
3. รันครบสามอย่างก่อนเปิด pull request

   ```bash
   cd web
   npx tsc --noEmit
   npx eslint src
   npm run build
   ```

4. เปิด pull request กรอกตามแม่แบบ แล้วรอ CI ผ่าน
5. merge แบบ squash เพื่อให้ประวัติบน `main` อ่านง่าย

## ข้อความ commit

ภาษาไทย บรรทัดแรกบอกผลลัพธ์ ไม่ใช่วิธีทำ ยาวไม่เกินประมาณ 70 ตัวอักษร
ถ้ามีรายละเอียดให้เว้นบรรทัดแล้วเขียนเป็นข้อ ๆ

```
ข้อ 3 USD: เหลือ 3 ตัวเลือก Break High · Sideway · Break Low

- dropdown ทั้ง 3 อินดี้ใช้ชุดเดียวกัน พร้อมคำอธิบายใต้ตัวเลือก
- คะแนนและเกณฑ์เลือกสถานการณ์เท่าเดิม
```

## แนวทางเขียนโค้ด

- **TypeScript ทั้งหมด** ชนิดข้อมูลกลางอยู่ที่ `web/src/lib/types.ts` แก้ที่นั่นก่อนเสมอ
- **คอมเมนต์ภาษาไทย** อธิบายว่าทำไม ไม่ใช่อธิบายว่าโค้ดทำอะไร
- **ข้อความที่ลูกค้าเห็นต้องไม่ฝังในหน้าเว็บ** ให้อยู่ใน `scenarios.ts` หรือคำของทีม
- **กติกาให้คะแนนอยู่ที่ `signal-forms.ts` ที่เดียว** เปลี่ยนวิธีคิดคะแนนเมื่อไหร่
  ต้องเปลี่ยน `RULES_VERSION` ด้วย ไม่งั้นสถิติย้อนหลังจะปนกัน
- **สีและระยะห่างมาจาก token ใน `globals.css`** อย่าใส่ค่าสีตรง ๆ ในหน้าเว็บ
- **แอนิเมชันต้องเคารพ `prefers-reduced-motion`** และต้องไม่บังการกดปุ่ม
- อย่า commit ค่าลับใด ๆ repo นี้เป็น public

## เมื่อแก้กติกาสัญญาณ

1. แก้ `web/src/lib/signal-forms.ts`
2. เปลี่ยน `RULES_VERSION` เป็นวันที่ที่แก้
3. อัปเดต [docs/signal-model.md](docs/signal-model.md) ให้ตรงกัน
4. เขียนในคำอธิบาย pull request ว่ากติกาเดิมกับใหม่ต่างกันอย่างไร
