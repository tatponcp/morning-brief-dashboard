import type { Metadata } from "next";
import { SectionView } from "@/components/SectionView";

export const metadata: Metadata = { title: "6 · Global Macro Signals" };

export default function Page() {
  return <SectionView id="macro" />;
}
